{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TopoDraft Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/packages/extension",
        "${workspaceFolder}/fixtures"
      ],
      "preLaunchTask": "build extension"
    }
  ]
}

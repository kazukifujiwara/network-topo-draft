// Phase 0 shell — the CustomTextEditorProvider, diagnostics publishing, and
// commands arrive in Phases 1–3 (plan §7). Deliberately does not import
// 'vscode' yet so Phase 0 needs no VSCode API surface.
export function activate(): void {
  // no-op in Phase 0
}

export function deactivate(): void {
  // no-op in Phase 0
}

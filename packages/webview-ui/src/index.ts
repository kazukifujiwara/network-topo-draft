// Phase 0 shell. The canvas UI (rendering, interactions, panels — ported from
// the frozen v7 reference) arrives in Phase 1+ and depends only on
// @topodraft/core and @topodraft/protocol (plan §4.1).
export const WEBVIEW_UI_SHELL = true;
